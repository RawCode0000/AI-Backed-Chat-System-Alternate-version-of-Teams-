rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read/write access to authenticated users or fallback for applet demo users
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
